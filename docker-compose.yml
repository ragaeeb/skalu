version: "3.9"

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - ALLOWED_ORIGINS=http://localhost:5173
      - MAX_CONTENT_LENGTH=10485760
    volumes:
      - /tmp/skalu-jobs:/tmp
